# 🖥️ بناء ملف EXE لنظام MOBPOS

النظام جاهز للتحول لتطبيق ويندوز كامل عبر **Electron** — كل التجهيزات موجودة،
والبناء يتم على جهاز ويندوز عادي بأمر واحد.

## المتطلبات (مرة واحدة)

- ويندوز 10/11 (64 بت)
- [Node.js 20+](https://nodejs.org) (حمّل النسخة LTS وثبّتها)

## خطوة البناء

```bash
# 1) انسخ المشروع على جهازك (أو اسحبه من الجيتهاب)
git clone https://github.com/msharaf221/MOBPOS-V2.git
cd MOBPOS-V2

# 2) ثبّت الحزم (هتحمّل إلكترون تلقائياً ~100MB)
npm install

# 3) ابنِ ملف EXE
npm run dist:win
```

بعد دقيقة إلى دقيقتين، تلاقي في مجلد **`release/`**:

| الملف | الوصف |
|---|---|
| `MOBPOS-Setup-1.0.1.exe` | **برنامج تثبيت** كامل (سطح مكتب + قائمة ابدأ) — وزّعه على المحلات |
| `MOBPOS-Portable-1.0.1.exe` | **نسخة محمولة** تشتغل بضغطة بدون تثبيت (من فلاشة مثلاً) |
| `latest.yml` | **ملف بيانات التحديث التلقائي** (يحتوي على رقم الإصدار والهاش) — **إلزامي لـ electron-updater** |

لو عايز النسخة المحمولة فقط: `npm run dist:win:portable`
لو عايز بناء ونشر مؤتمت إلى GitHub Releases مباشرة: `GH_TOKEN=your_token npm run dist:win:publish`

## التشغيل والتجربة أثناء التطوير

```bash
npm run electron:dev    # يبني الويب ويفتح نافذة التطبيق فوراً
```

## 🖥️ وضع الكيوسك (شاشة عرض المحل)

شغّل التطبيق بملء الشاشة بدون إمكانية الخروج:

```bash
MOBPOS.exe --kiosk
```

مفيد لشاشات العرض ونقاط البيع الثابتة. (من داخل التطبيق أيضاً يمكن التبديل عبر الجسر `window.mobpos.toggleKiosk()`.)

## 🔄 التحديث التلقائي (مهم جداً)

التطبيق المثبّت يفحص GitHub Releases تلقائياً عند التشغيل وكل 6 ساعات،
ويثبّت النسخة الجديدة عند إغلاق التطبيق — بدون أي تدخل منك.

⚠️ **شرط أساسي لعمل التحديث التلقائي:**
لكي يعمل `electron-updater` بنجاح، **يجب رفع ملف `latest.yml` المولد داخل مجلد `release/` جنباً إلى جنب مع ملف `MOBPOS-Setup-*.exe`** في نفس الـ GitHub Release.
بدون ملف `latest.yml`، لن يتمكن التطبيق من قراءة بيانات الإصدار الجديد أو التحقق من التجزئة (Hash) وسيفشل التحديث التلقائي بصمت.

### خطوات إصدار ونشر تحديث جديد:

**الطريقة المؤتمتة (الموصى بها):**

0. **مرة واحدة فقط:** فعّل ملف الـ workflow — محفوظ في `ci/release.yml` لأن توكن
   GitHub في جلسات Arena ما لوش صلاحية `workflows` فمش قادر ينشئ ملفات تحت
   `.github/workflows/`:
   ```bash
   npm run setup:ci                                  # بينسخه لـ .github/workflows/release.yml
   git add .github/workflows/release.yml
   git commit -m "ci: enable release workflow"
   git push
   ```
1. ارفع رقم الإصدار في `package.json` (مثلاً `1.0.1`) وأضف قسم الإصدار في `CHANGELOG.md`.
2. اعمل commit وارفع tag بنفس الرقم:
   ```bash
   git commit -am "release: v1.0.1"
   git tag v1.0.1
   git push origin main --tags
   ```
3. GitHub Actions (`.github/workflows/release.yml`) هيبني `Setup` + `Portable` + `latest.yml`
   على `windows-latest` وينشرهم على **GitHub Release** باسم `v1.0.1` — ومحتوى Release Notes
   بيتاخد تلقائياً من قسم الإصدار في `CHANGELOG.md`.
4. سيكتشف التطبيق عند العملاء التحديث تلقائياً ويقوم بتحميله وتثبيته ✅.

> الـ workflow بيرفض البناء لو رقم الـ tag مختلف عن `package.json`، وبيقف بخطأ
> لو `latest.yml` ما اتولّدش — عشان التحديث التلقائي ما يبوظش بصمت.

**الطريقة اليدوية (على جهاز ويندوز):**
1. ارفع رقم الإصدار في `package.json`.
2. ابنِ الحزمة: `npm run dist:win`.
3. أنشئ **GitHub Release** جديد على `msharaf221/MOBPOS-V2` بنفس رقم الإصدار (Tag: `v1.0.1`).
4. **ارفع الملفين معاً كـ Release Assets:**
   - `MOBPOS-Setup-1.0.1.exe`
   - `latest.yml` *(إلزامي)*

## ☁️ ملاحظة Google Drive داخل الـ EXE

التطبيق يشغّل سيرفراً محلياً داخلياً على `http://127.0.0.1:8420`
(ده بيخلي جوجل والتخزين يشتغلوا بشكل موثوق). لو بتستخدم النسخ لجوجل:

1. افتح [Google Cloud Console](https://console.cloud.google.com) → نفس مشروع الـ OAuth.
2. في إعدادات الـ OAuth Client، أضف `http://127.0.0.1:8420` لقائمة **Authorized JavaScript origins** بجانب رابط موقعك.
3. احفظ — الربط هيشتغل في الـ EXE والمتصفح معاً.

## 📁 فين بياناتي داخل الـ EXE؟

بيانات النظام (مبيعات، مخزون، ترخيص، نسخ احتياطية محلية) تُحفظ في:

```
%APPDATA%\mobpos\
```

- البيانات **معزولة تماماً** عن المتصفح — مسح المتصفح مش هيأثر عليها.
- الترخيص وبصمة الجهاز ثابتة لأن التطبيق دائماً على نفس العنوان المحلي.
- التطبيق **نسخة واحدة فقط** في أي وقت (مينفعش فتح نافذتين تتخانقوا على البيانات).

## 🔐 توقيع التطبيق (اختياري — لاحقاً)

ويندوز هيطلع تحذير "Windows protected your PC" أول مرة لأن الملف غير موقّع.
الحل التجاري: شراء شهادة كود ساينينج (Code Signing) وتفعيلها في `electron-builder.yml`.
بدونها التطبيق يشتغل عادي بعد ما العميل يضغط "More info → Run anyway".

## 🔁 إصدار نسخة جديدة يدوياً للعميل

1. غيّر الرقم في `package.json` (حقل `version`).
2. أعد `npm run dist:win`.
3. وزّع ملف Setup الجديد — عند العميل التثبيت فوق القديم بدون فقد بيانات.
