# المهام المؤجلة للإصدارات القادمة — TODO_LATER

## 1. تخزين لقطات النسخ الاحتياطي في مجلد ملفات خارجي (Offloading Backup Snapshots to Filesystem)

### السياق والوضع الحالي:
في الوقت الحالي، تُخزن النسخ الاحتياطية المحلية (حتى 7 لقطات) ككائنات JSON كاملة (`payload: BackupPayload`) داخل مخزن `backups` في قاعدة بيانات IndexedDB.
- هذا التخزين يضمن استقلالية التطبيق وسهولة الاسترجاع حتى في بيئة المتصفح الخالص بدون Electron.
- تم في الإصدار 1.0.5 استبعاد مخزن `backups` من حمولة التصدير السحابي والنسخ الاحتياطي (`exportAllData([STORES.backups])`) لتفادي قراءة الـ 7 لقطات السابقة وتضخم البيانات 8 أضعاف.

### سبب التأجيل وتفضيل خيار الفهارس (DB Version 5):
1. تم تفضيل ترقية مخطط IndexedDB وإضافة فهارس (`code` و `barcode` و `imei1` و `customerId` و `safeId`) مع ترقية `DB_VERSION = 5` كترقية آمنة وإضافية (Additive Migration) غير مدمرة وتخدم استقرار واستعلامات قاعدة البيانات.
2. نقل اللقطات إلى مجلد خارجي (`electron/app.getPath('userData')/backups`) يتطلب:
   - إنشاء قنوات IPC مخصصة للقراءة والكتابة والحذف وإدارة أذونات الملفات عبر نظام التشغيل (Windows/Linux/macOS).
   - توفير مسار بديل متوافق تماماً (Fallback) عند التشغيل في المتصفح أو بيئة الويب التي لا تملك وصولاً للملفات.
   - إدارة آلية تنظيف السنابات القديمة (Retention Pruning) من القرص مباشرة وتفادي سباقات الكتابة غير المتزامنة.

### خطة التنفيذ المقترحة في الإصدارات القادمة:
- إضافة IPC handler في `electron/main.cjs` باسم `mobpos:save-backup-snapshot` و `mobpos:read-backup-snapshot`.
- تخزين البيانات الوصفية فقط (`{ id, name, createdAt, size, auto, filePath }`) في مخزن `backups` بـ IndexedDB.
- قراءة الحمولة من `payload` القديم إن وجد (للتوافق مع النسخ القديمة)، وإلا قراءتها من الملف عبر IPC.
