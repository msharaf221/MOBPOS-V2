# 📋 مهام وتحسينات مؤجلة — المجموعة B (TODO_LATER)

تم تنفيذ وإغلاق كل البنود المتبقية من المجموعة B بتاريخ 2026-08-27.

---

## ✅ حالة البنود

1. ✅ **B1 — هشاشة بصمة الجهاز (Device Fingerprint Fragility)**
   - تم استبدال الاعتماد على هاش صارم واحد ببصمة v3 تحتوي 6 إشارات مستقلة.
   - تم تطبيق مطابقة بالأغلبية: قبول الجهاز عند تطابق 4 إشارات من 6 على الأقل.
   - في Electron تتم إضافة معرف نظام تشغيل ثابت ومجزأ عبر `preload`/IPC لتقوية الثبات.

2. ✅ **B2 — سباق التحديث في `useLocalStorage`**
   - تم تعديل `setValue` لاستخدام functional state updates عبر `setStoredValue(prev => ...)`.
   - يتم حفظ نفس القيمة الناتجة في React state و`localStorage` لتجنب فقدان التحديثات المتتابعة السريعة.

3. ✅ **B3 — تتبع `dist/` في Git**
   - تم إزالة `dist/` من التتبع عبر Git.
   - تمت إضافة `dist/` إلى `.gitignore` حتى لا تدخل مخرجات البناء في المستودع مرة أخرى.

4. ✅ **B4 — غياب HMR أثناء تطوير تطبيق Electron**
   - أصبح `npm run electron:dev` يشغل Vite dev server على الأصل الثابت `http://127.0.0.1:8420`.
   - يقوم Electron بتحميل خادم التطوير مباشرة بدلاً من تنفيذ build كامل في كل تشغيل.
   - تم الحفاظ على نفس المنفذ المطلوب لـ Google OAuth وIndexedDB مع دعم HMR.

5. ✅ **B5 — Content Security Policy (CSP)**
   - تمت إضافة CSP meta tag في `index.html`.
   - تمت إضافة ترويسة CSP من Electron للطلبات المحلية.
   - السياسة تسمح بالاتصال بسيرفر التفعيل، Google APIs، ونطاقات Supabase، مع دعم `data:` و`blob:` للصور والتقارير.

---

## ملاحظة حول B6

تم حذف B6 من هذه القائمة لأن ترقية PBKDF2 كانت مطبقة بالفعل في:

- `src/utils/passwords.ts`
- `src/license/crypto.ts`

النظام يستخدم PBKDF2-HMAC-SHA-256 بعدد دورات مرتفع مع Salt فريد وتوافق رجعي للهاشات القديمة.
