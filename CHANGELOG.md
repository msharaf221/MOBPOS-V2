# سجل التغييرات — MOBPOS

التنسيق متوافق مع [Keep a Changelog](https://keepachangelog.com/ar/1.0.0/)،
وأرقام الإصدارات تتبع [Semantic Versioning](https://semver.org/lang/ar/).

> workflow النشر (`.github/workflows/release.yml`) بياخد القسم الخاص بالإصدار
> من الملف ده ويحطه كـ Release Notes على GitHub.

## [Unreleased]

### إصلاحات — الحسابات الجانبية

- **«خزنة مستقلة» مع خزنة موجودة كانت بترفض الحفظ:**
  - لما بتختار تأثير «خزنة مستقلة» وتقوم باختيار خزنة موجودة (من غير ما
    تكتب اسم خزنة جديدة)، العملية كانت بترجع بخطأ «تعذر حفظ العملية» رغم إن
    النموذج بيسمح بذلك صراحةً. دلوقتي الاختيارين الشغالين: خزنة موجودة **أو**
    اسم جديد.
  - كتابة نفس اسم خزنة جديدة مرتين كانت بتنشئ **خزنتين بنفس الاسم** — دلوقتي
    الاسم الموجود بيتُستخدم زي ما هو.
- **«تحديث المدفوع» كان بيفشل بصمت وبيلخبط أرصدة الخزنات:**
  - تقليل المدفوع لعملية من غير تحصيلات سابقة كان **بيتقفل من غير ما يحفظ
    حاجة** (فشل صامت). دلوقتي بيشتغل وبيرجّع الفلوس صح.
  - التقليل كان بيخصم كامل الفرق من خزنة واحدة مهما كان توزيع التحصيلات،
    وبيشمل كمان «المدفوع المبدئي» اللي اتسجل كنوتة عند إضافة العملية و**عمرو
    ما دخل خزنة** — فكان ممكن الخزنة تروح في سالب بفلوس عمرو ما شافتها.
    دلوقتي المرتجع بيرجع **فقط للخزنات اللي اتحصّل/اتسدّد منها فعلاً**
    (الأحدث أولاً — LIFO)، وجزء النوتة بيتعدل من غير أي حركة كاش.
  - رفع المدفوع بيسجل في الخزنة المختارة زي ما هو، والعملية بترجع
    `SideAccountEntry | null` بدل `void`، فالواجهة بتعرض رسالة واضحة لو
    الحفظ فشل بدل ما المودال يقفل بصمت.
  - «إجمالي المدفوع» في مودال التحديث بقى مضبوط بحد أقصى المبلغ، وإرسال خزنة
    فاضية ما بيتعملش (كان بيوقف التحديث كله بصمت)، ولو آخر خزنة تحصيل
    اتمسحت بيترجع الخزنة الافتراضية بدل قائمة فاضية.
- **نموذج الإضافة:** رسالة واضحة لو «المدفوع» أكبر من المبلغ، ولو «خزنة
  مستقلة» من غير خزنة اختيار ولا اسم جديد. اختيار «التأثير المالي» بقى
  معطّل مع الديون (ليّا عنده/عليّا له) لأنه مالوش تأثير فيها، مع توضيح إن
  التحصيل بيتسجل من زرار التحديث ✓.
- **التصدير لـ Excel** بقى يحترم البحث وفلتر الحالة (كان بيصدّر كل العمليات
  مش المعروضة).
- **عمود التأثير** ما بيعرضش اسم خزنة تحت «بدون تأثير على المحل» للديون بعد
  التحصيل (كان متناقض)، وبيعرض «خزنة محذوفة» لو الخزنة اتمسحت.
- **المالية (سجل المعاملات):** أنواع `side_account` و`capital` و`customer_payment`
  و`wallet_deposit/withdrawal` كانت بتظهر بأسماء إنجليزية خام — اتضافت تسميات
  عربية ليها، مع فلترين جديدين «الحسابات الجانبية» و«رأس المال».

### جودة واختبارات

- فصل منطق تخطيط المرتجعات في دوال نقية بـ `src/utils/sideAccounts.ts`
  (`planSettlementReversal` بأسلوب LIFO مع اتجاه العملية، و`settledThroughSafes`)
  بدل منطق مضمّن في الـ hook.
- إضافة **8 اختبارات وحدة** في `src/utils/sideAccounts.test.ts` بتغطي التوزيع
  على أكتر من خزنة، الفصل داخل تسوية واحدة، الديون (payable)، تجاهل تسويات
  المرتجعات القديمة بدل «عكس العكس»، والصفوف غير الصالحة.

## [1.0.8]

### لوحة التحكم — كارت «المبيعات حسب الفئة»

- **تحليل مبيعات الشهر الحالي فقط:**
  - الكارت كان بيجمع مبيعات **كل الفترات** من أول استخدام البرنامج، فالأرقام
    كانت بتكبر للأبد ومفيش مقارنة حقيقية بين شهر وآخر.
  - دلوقتي الحسبة مقتصرة على **الشهر الحالي** (من أول لحظة في أول يوم للشهر
    لحد آخر لحظة فيه)، والكارت **يتصفّر تلقائيًا** مع بداية كل شهر جديد — حتى
    لو التطبيق مفتوح على مدار الشهر.
  - نفس نمط الفلترة الزمنية المستخدم في كروت اليوم/الشهر بلوحة التحكم، مع
    مراعاة صيغ التواريخ المخزنة (`ISO` وتواريخ بدون وقت تُقرأ بالتوقيت المحلي
    حتى لا ينزلق يوم من الشهر لشهر مجاور).
  - عنوان الكارت بقى تحت سطر صغير بيوضح النطاق: «مبيعات شهر (اسم الشهر) فقط»،
    ومركز الرسم بقى «إجمالي مبيعات الشهر»، والرسالة الودّية بقت
    «لا توجد مبيعات هذا الشهر بعد».
- **قائمة الفئات الجانبية مع «عرض المزيد»:**
  - القائمة بتعرض **أعلى 5 فئات مبيعًا** مرتبة تنازليًا حسب القيمة، وزرار
    «عرض المزيد» بيظهر الباقي ويتحول لـ«عرض أقل» — بدل قائمة طويلة بتكسّر
    شكل الكارت مع كترة الفئات.
  - الرسم الدائري (Donut) مرتب تنازليًا كمان، والفئات الصغيرة بعد الخامسة
    تتجمّع في شريحة **«أخرى»** بلون رمادي ثابت، فالرسم يفضل مقروء مهما كترت
    الفئات. وعند «عرض المزيد» تنفرد الشريحة لفئاتها الأصلية بنفس اللون.
  - النسب المئوية بقت تعرض «<1%» بدل «0%» للفئات الصغيرة جدًا.

### جودة واختبارات

- فصل منطق الكارت لدوال نقية في `src/utils/salesByCategory.ts` (نطاق الشهر،
  التجميع والترتيب، تجهيز صفوف الرسم والقائمة، النسب المئوية) بدل `useMemo`
  مضمّن في المكوّن.
- إضافة **18 اختبار وحدة** في `src/utils/salesByCategory.test.ts` تغطي حدود
  الشهر (أول/آخر لحظة، الشهر الماضي، الشهر الجاي، سنة تانية)، صيغ التواريخ
  المحلية، تجميع الفئات وترتيبها، تجميع «أخرى»، وحالات الإجمالي الصفري.

## [1.0.7]

### لوحة التحكم

- **إعادة تصميم كارت «المبيعات حسب الفئة»:**
  - الرسم البياني بقى دائري مفرّغ (Donut) بيعرض **إجمالي المبيعات في المنتصف** بلمحة واحدة.
  - إضافة وسيلة إيضاح (Legend) جنب الرسم بتوضح اسم كل فئة ونسبتها المئوية من إجمالي المبيعات.
  - تلميح (Tooltip) مخصص بيعرض اسم الفئة، قيمة مبيعاتها، ونسبتها المئوية معاً عند الوقوف على أي قطاع.
  - عرض رسالة ودّية «لا توجد مبيعات لعرضها بعد» بدل الكارت الفارغ لما ميكونش فيه مبيعات لسه.

## [1.0.6]

### سلامة البيانات والمعاملات الذرية

- **سلامة فروقات الحالة (`Diff Integrity & Copy-On-Write`):**
  - استبدال التعديلات العينية المباشرة (`in-place mutations`) بتحديثات دفعية غير قابلة للتغيير (`Copy-On-Write`) في جميع عمليات الحالة المالية والمخزنية (`createSale`، `processSaleReturn`، `recordStockWaste`، `addSideAccountEntry`، `updateSideAccountEntry`).
  - ضمان اكتشاف محرك `diffStore` لكافة السجلات المعدلة داخل `IndexedDB` دون إغفال أي تعديل في المخزون أو رصيد الخزائن.
- **معاملات ذرية متعددة المخازن (`Multi-Store Atomic Transactions`):**
  - إضافة دالة `persistMultiStoreDeltas` لتنفيذ كتابات الفروقات عبر عدة مخازن داخل معاملة `readwrite` ذرية واحدة في IndexedDB.
  - إضافة مساعد المعاملات مع مسار التراجع (`runAtomicTransaction`) لضمان عدم حدوث تباين جزئي بين المخازن عند حدوث أي خطأ أثاء الحفظ.
- **ترقية مخطط IndexedDB إلى v5 وفهارس الاستعلام السريع:**
  - ترقية آمنة وغير مدمرة مع الحفاظ الكامل على بيانات المستخدمين.
  - إضافة فهارس ثانوية متقدمة (`code`, `barcode`, `categoryId`, `imei1`, `inventoryId`, `status`, `customerId`, `invoiceNumber`, `safeId`, `referenceId`) لتسريع البحث وعمليات الربط.
  - توثيق المعمارية المستقبلية لنقل لقطات النسخ الاحتياطي إلى ملفات القرص الصلب (`userData`) في `TODO_LATER.md` و`README.md`.

### توحيد التنسيقات والأمان

- **توحيد محرك التنسيق (`Intl Formatters`):**
  - إزالة كافة القيم الثابتة المكتوبة يدويًا (`ar-EG` و`EGP`) واستبدالها بنظام التنسيق الموحد `src/utils/format.ts` عبر جميع شاشات التطبيق ومطبوعاته (`POS`, `Sales`, `Finance`, `Dashboard`, `Layout`, `LicenseExpired`, `MasterAdmin`, `InventoryAudit`, `SideAccounts`, `alerts`, `reports`, `barcode`, `backup`, `updateCheck`).
  - دعم الأرقام اللاتينية الموحدة مع الواجهة العربية RTL.
- **حماية تصدير الملفات:**
  - إضافة حماية متقدمة من حقن الصيغ والمعادلات الخبيثة (`Formula Injection Protection`) في تصدير CSV.

### اختبارات وضمان الجودة

- إضافة اختبارات وحدوية شاملة لسلامة الفروقات (`storeDiff.test.ts`)، المعاملات الذرية (`atomicTransaction.test.ts`)، ومنسقات النظام (`format.test.ts`).
- جميع الاختبارات (44 اختباراً) تمر بنجاح 100% مع فحص TypeScript نظيف تماماً.

## [1.0.5]

### أداء — النظام بقى يتحمّل كتالوج كبير من غير لاج

المشكلة اللي اتحلّت: كل تعديل في أي قائمة كان بيعيد كتابة **المخزن بالكامل** في
IndexedDB (مسح + إعادة حفظ كل السجلات)، فكل منتج جديد كان بيزوّد حجم الكتابة
بشكل تربيعي. حوالي 250–300 منتج كان كفاية إن الإضافة «تعلّق» ثواني وترجع تكمّل.

- **كتابة الفروقات بس (`delta writes`):** أي تعديل بيبعت للسجل المتغيّر أو الجديد
  `put`، والممسوح `delete` — بدل مسح المخزن وإعادة كتابته. إضافة 1000 منتج
  واحد ورا التاني نزلت من ~500,500 عملية كتابة إلى 1000 بالضبط (أقل بـ 500 مرة)،
  ومعها تضخّم حجم ملف الداتا اختفى. القياس العملي: 1000 إضافة في 199ms بدل 4288ms. لو التغيير لمس المخزن كله (استيراد/جرد)
  لسه بيستخدم الكتابة الكاملة لأنها أرخص في الحالة دي.
- **دمج الكتابات المتلاحقة:** الضغطات السريعة على «إضافة» أو الكتابة في الإعدادات
  بتتجمع في معاملة واحدة لآخر قيمة بدل طابور معاملات ورا بعض.
- **`flushPendingWrites()`:** النسخ الاحتياطي والمزامنة والاستعادة من ملف بيخلصوا
  أي كتابة معلّقة الأول، فمابقاش ممكن ينسخوا/يداوسوا لقطة قديمة.
- **فهارس بدل المسح المتكرر:** كمية IMEI المتاحة بتتحسب من فهرس واحد
  (`buildImeiStockIndex`) بدل مسح قائمة الـ IMEI لكل صف — في جدول المخزون،
  الإحصائيات، محرك التنبيهات، شبكة POS، وشاشة الجرد. كمان أسماء الفئات والعملاء
  والمنتجات بقوا `Map` بدل `.find()` لكل صف.
- **منسّقات `Intl` متخزّنة:** `formatCurrency`/`formatDate` بقوا في
  `src/utils/format.ts` بـ formatter واحد لكل (لغة + عملة) بدل بناء formatter
  جديد لكل خلية — كان لوحده بياخد ~28ms في ريندر جدول 256 صنف وبيتكرر مع كل
  حرف في البحث.
- **تقسيم صفحات (Pagination) في كل القوائم:** الجدول كان بيرسم كل الصفوف في الـ DOM
  مرة واحدة. بقى شريط تقسيم موحّد (20/50/100/250 في الصفحة، وحجم الصفحة بيفتكر
  اختارك) في: المخزون، المبيعات، العملاء، الموردين، أجهزة IMEI، الحسابات الجانبية،
  المستخدمين، وحركة الخزينة (بدل `slice(0, 50)` اللي كانت بتخبي باقي المعاملات).
  كروت لوحة المعلومات بقى ليها «عرض الكل»، وأعمدة الصيانة ليها حد أقصى + «عرض كل
  التذاكر»، وشبكة أصناف POS بتحمّل 60 كارت و«عرض المزيد».

### إصلاحات

- **إضافة منتج بترفض بصمت:** المتجر كان بيرفض أي منتج من غير «الكود» أو بكود/باركود
  مكرر من غير أي رسالة، والفورم كان بيقفل وبيفضّي الحقول — المستخدم كان يفتكر إن
  النظام «علّق» أو إن المنتج اتضاف. دلوقتي: الكود بيتولّد تلقائيًا لو فاضي،
  تكرار الكود بيظهر تحذير أحمر قبل الحفظ، ولو الرفض حصل في المتجر بتظهر رسالة
  والسجل بيضل مفتوح. نفس الإصلاح في التعديل والحذف (بيوضح سبب الرفض: مستند مرتبط
  بفاتورة/IMEI/صيانة).
- **توليد الباركود:** فحص التكرار بقى `Set` (سريع على الكتالوجات الكبيرة)، والمقارنة
  بتتم بعد `trim`، والـ fallback اللي كان بيرجّع باركود **بدون خانة مراجعة EAN-13
  صحيحة** اتلغى — أي باركود مولّد دلوقتي EAN-13 سليم 100%.
- **بحث/فلترجر لا تنهار على صفوف قديمة:** `code`/`barcode` الفارغة (من نسخ أو ملفات
  استيراد قديمة) كانت بتسبب `undefined.includes` في الفلترة.
- **نسخة احتياطية أخف:** `exportAllData` مبقاش يقرأ مخزن `backups` (7 لقطات كاملة
  من الداتا) ثم يحذفه من الحزمة — كل نسخة احتياطية كانت بتقرأ الداتا ~8 مرات.
- **عمود «مسلّم» في الصيانة:** كان بيعرض أقدم 10 تذاكر فقط؛ بقى الأحدث أولًا مع
  إمكانية عرض الباقي.

### اختبارات

- ملف جديد `src/utils/barcode.test.ts` (7 اختبارات): صحة EAN-13 لكل باركود مولّد،
  عدم التكرار، وتطابق `buildImeiStockIndex`/`groupCountsBy` مع المنطق القديم.
- `npm test`: 34 اختبار ناجح، و`npm run typecheck` نظيف.

## [1.0.4]

### إصلاحات وتحسينات أمنية

- تقوية التحقق من مفاتيح الترخيص وبصمة الجهاز، وإيقاف التحقق الخلفي قبل منح الوصول عند وجود رفض صريح من السيرفر.
- جعل حفظ Activation Server ذرياً وفورياً مع توكن عميل إلزامي، تحقق من المدخلات، حماية rate limit، وتنظيف آمن للبيانات.
- منع حقن HTML/XSS في طباعة الباركود وتقييد عدد النسخ والمدخلات.
- جعل استعادة النسخ الاحتياطية والمزامنة تتحقق من السجلات وتستخدم معاملات IndexedDB ذرية.
- إضافة تحقق مركزي للمبيعات والمرتجعات والمخزون وIMEI والصيانة والخزائن والحسابات والعملاء والموردين.
- تقوية Electron والسيرفر المحلي وحماية كلمات المرور من مدخلات مهيأة لإحداث استهلاك زائد للمعالج.

## [1.0.3]

### إضافات

- **إدارة الرخصة من جوه التطبيق (الإعدادات ← الترخيص والتحديثات):**
  - عرض بيانات الرخصة الحالية: الباقة، اسم المحل، تاريخ الانتهاء أو «مدى الحياة»،
    حالة واضحة (شغّالة / قرب تنتهي ≤ 7 أيام / منتهية)، معرّف المفتاح، ومعرّف الجهاز
    (يسهّل التواصل مع الدعم الفني).
  - زرار «إدخال مفتاح جديد (تغيير/ترقية)» يفعّل المفتاح بنفس مسار التفعيل الحالي،
    ويرسائل عربية واضحة عند الخطأ (مثلاً «المفتاح ده مربوط بجهاز تاني — كلم مزود النظام»).
  - زرار «إلغاء التفعيل» مع تأكيد ينقل لشاشة التفعيل.
- **كارت «تحديثات التطبيق»:** يعرض الإصدار الحالي + زرار «افحص التحديثات» اليدوي
  (يظهر في النسخ المثبتة فقط). أُصلح الفحص ليعمل مع واجهة `electron-updater` v6
  (`isUpdateAvailable` بدل فحص `updateInfo` الذي كان موجوداً دائماً)، ويعرض رسائل
  واضحة: تحديث متاح / أحدث إصدار / فشل شبكة.
- الفحص التلقائي (بعد 5 ثوانٍ + كل 6 ساعات) لم يتغيّر — الزرار يدوي مكمّل وليس بديلاً.

## [1.0.2]

### إضافات

- **تسجيل خروج تلقائي عند قفل التطبيق أو النظام (حماية البيانات):**
  الجلسة لم تعد محفوظة بعد إغلاق البرنامج — كل ما يُفتح النظام مرة أخرى،
  تظهر شاشة تسجيل الدخول (اسم المستخدم + كلمة المرور) ولا أحد يستطيع
  الوصول للبيانات بحساب كان فاتح قبل القفل. ده بيحمي المحل لو حد
  استخدم الجهاز بعد قفله أو في غيبة صاحبه.

## [1.0.1]

### إصلاحات

- **الإشعارات كانت معلّقة على رقم ثابت (مثلاً 3) ومفيش أي طريق يمسحها.**
  التطبيق أصلاً ما كانش بينشئ أي إشعار، والعداد كان بيعد صفوف قديمة واصلة من
  استرجاع نسخة احتياطية. الصفوف القديمة دي (اللي ملهاش `source`) بتتخبّى دلوقتي
  تلقائياً، والعداد بقى بيعكس الحاجة الحقيقية بس.
- إضافة **زرار «تعليم الكل كمقروء»** و**زرار «مسح كل الإشعارات»** في قائمة الإشعارات،
  وزرار **إخفاء (×)** لكل إشعار بمفرده.

### إضافات

- **إشعارات حقيقية من بيانات المحل** بتتولد تلقائياً وتختفي لوحدها أول ما المشكلة تتحل:
  - مخزون منخفض (نفس قاعدة «الكمية الحقيقية» المستخدمة في المخزون ولوحة التحكم)،
  - ضمانات أجهزة قربت تنتهي (خلال 30 يوم)،
  - تذاكر صيانة متأخرة (مفتوحة أكتر من 7 أيام)،
  - مديونيات العملاء المستحقة.
- الضغط على أي إشعار **بيفتح الصفحة المرتبطة بيه** (المخزون / IMEI / الصيانة / العملاء).
- عرض **وقت الإشعار** بصيغة نسبية عربية (منذ 3 ساعات / منذ يومين).
- قائمة الإشعارات بقت **بتقفل** لما تدوس بره أو تدوس Escape، والعداد بيعرض `+9`
  لما يزيد عن 9.
- رقم الإصدار في **الإعدادات ← عن النظام** بقى بيتقرأ من `package.json` مباشرة
  بدل ما يكون مكتوب يدوياً.
- **نشر مؤتمت:** رفع tag باسم `v1.0.1` على GitHub بيبني ملف التثبيت وينشره مع
  `latest.yml` على Releases، فينزل التحديث لكل العملاء أوتوماتيك.

## [1.0.0]

- أول إصدار كامل من النظام.
